import extensoes.lastChar
import extensoes.lastWord


fun main() {
    fun main() {
        val msg = "Oi, mundo!"

        println(msg)
        println(msg.lastChar())
        println(msg.lastWord())
    }
}